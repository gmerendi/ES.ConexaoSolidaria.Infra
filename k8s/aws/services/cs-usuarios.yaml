apiVersion: apps/v1
kind: Deployment
metadata:
  name: cs-usuarios-api
  labels:
    app: cs-usuarios-api
    project: conexao-solidaria
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1        # sobe 1 pod novo antes de derrubar o antigo
      maxUnavailable: 0  # garante zero downtime
  selector:
    matchLabels:
      app: cs-usuarios-api
  template:
    metadata:
      labels:
        app: cs-usuarios-api
        project: conexao-solidaria
    spec:
      containers:
        - name: cs-usuarios-api
          image: 641008666847.dkr.ecr.us-east-1.amazonaws.com/cs-usuarios-api:latest
          # imagePullPolicy: Never → usa a imagem local buildada pelo docker-compose
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080      

          # ── Variáveis ──────────────────────────────────
          env:
           #AspnetCore
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: ASPNETCORE_ENVIRONMENT

            - name: ASPNETCORE_URLS
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: ASPNETCORE_URLS


            # Application Type - LOCAL ou LAB
            - name: Application__Type
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: APPLICATION_TYPE

            #JWT
            - name: Jwt__SecretKey
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: JWT_SECRETKEY

            - name: Jwt__Issuer
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: JWT_ISSUER

            - name: Jwt__Audience
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: JWT_AUDIENCE

            - name: Jwt__ExpirationHours
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: JWT_EXPIRATION_HOURS

            - name: AES__KEY
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: AES_KEY

            #Rabbitmq
            - name: RabbitMq__Password
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: RABBITMQ_PASSWORD

            - name: RabbitMq__Username
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: RABBITMQ_USERNAME

            - name: RabbitMq__Host
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: RABBITMQ_SVC

            - name: USER_CREATED_QUEUE
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: RABBITMQ_USER_CREATED_QUEUE

            #Database
            - name: USUARIOS_DB_SVC
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: USUARIOS_DB_SVC
            - name: USUARIOS_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: USUARIOS_POSTGRES_DB
            - name: USUARIOS_DB_PASS
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: USUARIOS_POSTGRES_PASSWORD
            - name: USUARIOS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: cs-secrets
                  key: USUARIOS_POSTGRES_USER
            - name: DYNAMODB_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: DYNAMODB_SERVICE_URL
            - name: REDIS_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: REDIS_SERVICE_URL
            - name: ELASTICSEARCH_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: ELASTICSEARCH_SERVICE_URL

            #ConnectionStrings
            - name: ConnectionStrings__Database
              value: "Host=$(USUARIOS_DB_SVC);Database=$(USUARIOS_DB_NAME);Username=$(USUARIOS_DB_USER);Password=$(USUARIOS_DB_PASS)"

            - name: ConnectionStrings__AuditLog
              value: $(DYNAMODB_SERVICE_URL)

            - name: ConnectionStrings__Redis
              value: $(REDIS_SERVICE_URL)

             #Logs
            - name: CustomLogging__LogInfo
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: CUSTOMLOGGING_LOGINFO
            - name: CustomLogging__LogError
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: CUSTOMLOGGING_LOGERROR
            - name: CustomLogging__LogWarning
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: CUSTOMLOGGING_LOGWARNING
            # Envia todos os logs para o DynamoDB. Eventos são sempre enviados
            - name: CustomLogging__SendLogToDB
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: CUSTOMLOGGING_SENDLOGTODB

            #AWS
            - name: AWS_REGION
              valueFrom:
                configMapKeyRef:
                  name: cs-configmap
                  key: AWS_REGION
          
          envFrom:
            - secretRef:
                name: cs-aws-credentials

          # ── Recursos ────────────────────────────────────────────────────
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          # ── Liveness Probe ───────────────────────────────────────────────
          # Se falhar: Kubernetes reinicia o container
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1

          # ── Readiness Probe ──────────────────────────────────────────────
          # Se falhar: pod sai do Service até estar pronto
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1